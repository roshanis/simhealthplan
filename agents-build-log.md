# Agents Build Log
<!-- Auto-generated by vibecode dual-agent workflow -->
<!-- Format: append entries chronologically, newest at bottom -->

## [AGENT: Claude Reviewer] [2026-08-01T02:55Z]
### Action: Code Review
### Files reviewed: pipeline/choice_model/plan_attributes.py, pipeline/choice_model/utility.py, pipeline/choice_model/choice_sets.py, pipeline/choice_model/calibrate.py, pipeline/choice_model/schemas.py, pipeline/tests/test_choice_model_plan_attributes.py, pipeline/tests/test_choice_model_utility.py, pipeline/tests/test_choice_model_choice_sets.py, pipeline/tests/test_choice_model_calibrate.py, Makefile

#### Summary
New, from-scratch Phase 3 implementation (multinomial-logit choice model + Year-1 calibration). Logic for MOOP/star imputation, D-SNP/I-SNP/C-SNP eligibility + prior-renormalization, and the calibration optimizer is correct and well-tested (270/270 pipeline tests green, including 7 real-data integration acceptance tests). `utility.py` is genuinely pure (verified by an explicit module-introspection test). Found and fixed two minor issues during self-review before this write-up: an unused variable in `calibrate.calibrate()`, and a gap in explicit "excluded SNP plan gets exactly zero probability" test coverage tying `choice_sets` + `utility` together end-to-end (both fixed; suite re-verified green).

#### Issues Found

| Severity | File | Line | Issue | Recommendation |
|----------|------|------|-------|----------------|
| 🟢 NITPICK | pipeline/choice_model/calibrate.py | ~410 (now fixed) | `plans_metadata` was assigned from `_load_plans_2024()` but never used | Fixed: renamed to `_` |
| 🟢 NITPICK | pipeline/choice_model/plan_attributes.py | `build()` | `other_year_landscape_df` (a full parquet load) is always loaded even when year==2025 and the crosswalk is unused for that direction — harmless (small file, ~94 rows) but slightly wasteful I/O | Optional: skip the load when `crosswalk_df` will be forced to `None`; not worth the added branching complexity for a 94-row parquet |
| 🟢 NITPICK | pipeline/choice_model/calibrate.py | `predicted_2024_counts` | `totals.get(key, 0.0)` inside the accumulation loop is defensive but `totals` is always pre-seeded with every plan_key + outside_key, so the `.get` fallback path is dead code | Low priority; harmless, arguably good defensive coding against a future caller passing a stale `plans` list |
| 🟢 NITPICK | pipeline/choice_model/plan_attributes.py | `impute_star_ratings` | No `imputed_star`-style test for the "all ratings unparseable" degenerate fallback (median → 3.5 constant) | Covered indirectly by real data (no year has 100% unrated plans); acceptable given it's a defensive-only branch |

No BLOCKER or WARNING-severity issues found.

#### Correctness deep-dive (per review brief)

- **MOOP imputation**: crosswalk backfill correctly restricted to `disposition == "renewed"` only (verified against real data: 26/29 backfilled, 3/29 — the "consolidated" plans — fall to type-median, matching the module docstring's documented split). Type-median fallback is computed over *originally* non-null values only (not crosswalk-backfilled ones), avoiding compounding imputation into another plan's median — this was caught and fixed during initial TDD (test `test_impute_moop_falls_back_to_type_median_when_not_renewed` initially failed against a naive implementation that included backfilled values in the median pool).
- **Star imputation**: placeholder-string parsing (`parse_star_rating`) correctly returns `None` for all 4 case variants across both years' CMS placeholder text; type-median fallback with an overall-median safety net for the pathological all-unrated case.
- **D-SNP/I-SNP/C-SNP eligibility**: `choice_sets.is_eligible` correctly implements all three rules; D-SNP is documented as a real CMS rule while I-SNP (age_band==85+) and C-SNP (attitude_segment==health_status_driven) are explicitly documented as proxies in the module docstring, `CHOICE_SET_RULES` metadata (`"proxy": true/false`), and consequently in `coefficients.json`'s `choice_set_rules` block. `renormalize_prior_plan` correctly treats the outside option as always-eligible and has a defensive degenerate-case fallback (all mass → outside) that can't currently be reached with real data but guards against a future rule change that could zero out an archetype's entire eligible set.
- **Real-data renormalization fidelity**: the required test (post-renormalization aggregate shares reproduce actual 2024 within 1pp size-weighted MAE) passes at 0.44pp; diagnostic run shows 1.34pp of eligibles' Year-1 prior mass gets renormalized (0.13pp I-SNP-driven, 1.20pp C-SNP-driven), confirming the impact is real but modest.
- **utility.py purity**: confirmed via `vars(utility)` introspection test — only plain-data constants (`SEGMENT_MULTIPLIERS`, `BASE_COEFFICIENT_NAMES`, scaling constants) and pure functions are module-level; no I/O, no mutable shared state, no numpy/pandas dependency. Straightforward to port 1:1 to TypeScript as intended.
- **Calibration optimizer**: multi-start L-BFGS-B with per-start L2 regularization anchored to that start's own prior (not a single shared anchor) is a deliberate, documented design choice — the loss surface here is genuinely multi-modal (traced to a dominant D-SNP plan holding ~31% of its restricted eligible population's prior mass, which a softmax linear in prior probability can only reproduce with a much larger inertia coefficient than the flatter non-SNP market needs). This was diagnosed empirically (see calibration session notes) before arriving at the two-anchor multi-start design and `L2_LAMBDA = 1e-6`. Final fit: size-weighted MAE 0.18pp (bar: ≤0.30pp), outside-share error 0.0016pp (bar: ≤2pp), deterministic (byte-identical reruns verified), all coefficient signs respect the documented bounds.
- **Pydantic schema usage**: `PlansFile2024`/`PlansFile2025`/`CoefficientsFile` all use `extra="forbid"`, so any drift between `_to_records()`'s emitted fields and the schema fails loudly at build time (`model_validate` is called inside `build_all()`/`build()` before writing to disk, not just in tests). Confirmed working against real data via integration tests.
- **Security**: no shell/SQL/eval, no untrusted-input parsing (all JSON/parquet reads are of pipeline-controlled, already-validated interim/processed files under `settings.*_DIR`), no secrets touched. Not applicable in any meaningful sense for this ETL/calibration code.

#### Test Coverage
- 270/270 pipeline tests pass (170 pre-existing + 100 new: 44 plan_attributes, 20 utility, 16 choice_sets, 20 calibrate — after this review's +3 parametrized addition to choice_sets and the plans_metadata fix).
- Real-data integration tests (marked `pytest.mark.integration`, run since `data/interim/*.parquet` are present in this checkout) cover: exact 93/94 plan counts, MOOP fully populated post-imputation, exact imputation method counts (26 crosswalk / 3 type-median), schema validation, byte-identical deterministic rebuilds, the 1pp renormalization-fidelity bar, and all Phase 3 acceptance bars (MAE ≤0.30pp, outside-share ≤2pp, coefficient signs, probabilities-sum-to-1 for every real archetype).
- Property-style tests present for all four spec-required properties: probabilities sum to 1, premium monotonicity, exact-zero SNP exclusion (now end-to-end across choice_sets+utility), and segment-multiplier directional shifts (all 4 named segments).

#### Verdict
- [x] APPROVED — ready to merge

### Recommendations / Next steps:
1. None blocking. The two nitpicks below "no BLOCKER/WARNING" are optional polish, not required before considering Phase 3 done.
2. When Phase 4 (TypeScript port) begins, port `utility.py` and `choice_sets.py` first and diff their outputs against `coefficients.json`'s `base_coefficients`/`segment_multipliers`/`choice_set_rules` on a handful of real archetype/plan pairs as a golden-output regression check.
3. Consider whether the calibration's discovered multi-modality (documented in calibrate.py's `STRONG_INERTIA_PRIOR_COEFFICIENTS` comment) is worth a short note in the eventual Phase 3 write-up for stakeholders — it's a genuine finding about this market (one dominant D-SNP plan concentrates dual-eligible enrollment), not just an optimizer quirk.

## [AGENT: Claude] [2026-08-01T03:32Z]
### Action: Implement Phase 4 (LLM persona layer)
### Files changed: pipeline/choice_model/predict.py (new), pipeline/llm/persona_prompts.py (new), pipeline/llm/blend.py (new), pipeline/llm/run_persona_pass.py (new), pipeline/tests/test_choice_model_predict.py (new), pipeline/tests/test_llm_persona_prompts.py (new), pipeline/tests/test_llm_blend.py (new), pipeline/tests/test_llm_run_persona_pass.py (new), Makefile (wired `make personas`)

### Diff summary
- `choice_model/predict.py`: pure Year-2 (2025) logit baseline, reused by Phase 5. Maps each archetype's `prior_plan_year1` through `plan_crosswalk_2024_2025.parquet` (renewed/renumbered/consolidated -> successor 2025 plan_key, consolidated old plans summed onto a shared successor) to a Year-2 prior. Terminated plans (9 of 93) get a dedicated `NO_SUCCESSOR_BUCKET` sentinel key -- neither a real plan_key nor the outside key -- so `choice_sets.renormalize_prior_plan` drops that mass entirely instead of redistributing it onto any specific surviving plan or the outside option (verified directly against real data: `test_real_terminated_plans_hold_no_inertia_for_any_surviving_plan`). Reuses `choice_sets.eligible_plans`/`renormalize_prior_plan` and `utility.choice_probabilities` unchanged -- no reimplementation of Phase 3 logic.
- `llm/persona_prompts.py`: CHOICE (`y2_choice_v1`) and BACKSTORY (`backstory_v1`) structured-output prompt builders, plus `build_consideration_set` (K=8: top eligible-and-crosswalk-mapped prior-mass plan + top-2 2025 enrollment + fill by logit probability, deduped, always restricted to the archetype's D-SNP/I-SNP/C-SNP-eligible set so a non-dual archetype's set can never contain a D-SNP plan -- verified both with synthetic fixtures and a real-data test across all 80 archetypes). CHOICE schema enum-restricts `ranked_plan_keys` to the exact alternative set (consideration set + outside) but deliberately omits `minItems`/`maxItems` (not confirmed part of OpenAI's strict-mode JSON Schema subset); the system prompt instructs a full ranking instead, and `run_persona_pass._sanitize_ranked_plan_keys` defensively repairs whatever comes back into a valid permutation regardless. BACKSTORY prompt explicitly instructs against mechanically deriving the persona's name from race/ethnicity while still grounding the backstory in age/income/traits.
- `llm/blend.py`: pure rank-bonus (`ALPHA=0.35`, linear rank 1..K -> alpha..0) and inertia-modulation (`clamp(1.15 - 0.30*switching_propensity, 0.85, 1.15)` multiplier on the fitted `b_inertia`) blend, re-softmaxed via `utility.plan_utility`/`outside_utility` (reused, not reimplemented). Both required bounded-deviation properties verified against all 80 real archetypes with a representative (logit-agreeing) mock LLM output: per-archetype L1 distance < 0.35 and aggregate weight-averaged share shift < 5pp (both comfortably inside bound in practice).
- `llm/run_persona_pass.py`: orchestrates the 80-archetype x 2-call (CHOICE + BACKSTORY) pass through the existing `llm.client.LLMClient` (cache-first, budget-capped -- reused, not reimplemented) and writes `data/processed/personas.json` + `data/processed/y2_predictions.json` (logit-only and blended aggregate 2025 shares side by side). Adds a pre-flight `ProjectedBudgetExceededError` check (computed from cache-key file existence, zero network calls) that fails loudly *before* any of the 160 planned requests is attempted if the projected new-call count would exceed the client's remaining budget -- stronger than `LLMClient`'s own per-call `BudgetExceededError`, which only fires mid-run.
- `Makefile`: `make personas` now runs `python -m llm.run_persona_pass` (was a placeholder echo).

### Test coverage
355/355 pipeline tests pass (270 pre-existing + 85 new). New integration tests (real committed archetypes.json/plans_2024.json/plans_2025.json/coefficients.json/plan_crosswalk_2024_2025.parquet, mocked LLM client, no network) cover: crosswalk disposition counts (74/9/7/3) and terminated-mass-no-inertia against real data; D-SNP exclusion from consideration sets across all 80 archetypes; the two blend bounded-deviation properties; full 80-archetype call-count accounting (160 planned calls, confirmed <=500 budget); warm-cache rerun making exactly 0 new network calls; the budget pre-flight check raising before any call when under-budget and *not* raising when the cache already covers the shortfall; deterministic output given fixed mock responses; personas.json/y2_predictions.json artifact shape and write-to-disk.

### Live run
`pipeline/.env` does not exist and no `OPENAI_API_KEY` is set anywhere in the environment -- per the live-run policy, no network call was attempted. Verified `make personas` is wired correctly end-to-end: it fails fast with `openai.OpenAIError: Missing credentials...` at the first attempted network call (no cache files or output artifacts were written by the failed attempt -- confirmed via `git status`/directory listing). All tests pass keyless. Once `OPENAI_API_KEY` is added to `pipeline/.env`, `make personas` should make ~160 calls (well under the 500 budget) and a rerun should make 0 new calls.

### Recommendations / Next steps:
1. When `OPENAI_API_KEY` becomes available, run `make personas` live, spot-check a handful of `personas.json` backstories/names for the "no mechanical race-to-name derivation" instruction actually holding up in practice (schema/prompt-level enforcement only goes so far), then rerun to confirm the warm-cache 0-new-calls property holds against the real cache too (not just the mocked-client tests).
2. Phase 5 should reuse `choice_model.predict.predict_all`/`predict_for_archetype` directly (already the "reused by Phase 5" baseline) rather than recomputing the Year-2 logit forward pass.
3. `OUTSIDE_KEY_YEAR2 = "other_or_no_ma_2025"` is a new, deliberately distinct constant from Phase 2/3's `other_or_no_ma_2024` -- worth a one-line callout in any future TypeScript port docs so the port doesn't assume a single hardcoded outside-option key across both years.

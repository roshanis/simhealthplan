OPENAI_API_KEY=

# Optional: only used by the secondary api.census.gov route. The primary ACS
# route downloads keyless flat files from www2.census.gov and needs no key.
# Sign up (if wanted): https://api.census.gov/data/key_signup.html
CENSUS_API_KEY=
